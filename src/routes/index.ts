import { Router } from 'express';

const router = Router();
export default router;

/**
 * 1. Make respective route folder in this directory
 * 2. Import the router
 * 3. Put it like this: router.use(<route-name>, imported router)
 */
console.log('test husky')
